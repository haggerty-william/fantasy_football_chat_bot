# Copy to secrets.env (ignored by Git) and fill in locally.
# Use KEY=value without surrounding quotes. Never commit the filled-in file.
DISCORD_WEBHOOK_URL=
# ESPN cookies are needed only for private leagues; otherwise keep values empty.
ESPN_S2=
SWID=
